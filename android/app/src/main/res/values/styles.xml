<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!--
          注意: 以前は "android:background" が指定されていたが、これはウィンドウの背景を
          制御する属性ではないため実際には何も効いていなかった(常にOS標準の背景色が出ていた)。
          Android 12以降のSplashScreen APIで背景色を変えるには windowSplashScreenBackground を使う。
          アイコン自体はアプリのランチャーアイコン(ic_launcher)がOSにより自動で使われる。
        -->
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
    </style>
</resources>